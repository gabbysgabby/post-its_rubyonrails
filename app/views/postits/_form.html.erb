<%= form_for @postit do |f| %>
<script>
var ready;
ready = function(){
// call sortable on our div with the sortable class
$('.sortable').sortable();
}
</script>

  <% if @postit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@postit.errors.count, "error") %> prohibited
        this postit from being saved:
      </h2>
      <ul>
        <% @postit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <body>
    <ul>
      <li>
          <h2>Post-it #1</h2>
          <p>
            <%= f.label :title %><br>
            <%= f.text_field :title %>
          </p>

          <p>
            <%= f.label :text %><br>
            <%= f.text_area :text %>
          </p>

          <p>
            <%= f.submit %>
          </p>

        <% end %>
      </liv>
    </ul>
  </body>
